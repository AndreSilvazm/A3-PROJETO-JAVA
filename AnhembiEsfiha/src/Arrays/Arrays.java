package Arrays;

import Model.Produto.PrincipalProdutos;

public interface Arrays {
    public void Inserir(PrincipalProdutos produtos);
    public void Exibir();
    public PrincipalProdutos[] NovoArray();
}
